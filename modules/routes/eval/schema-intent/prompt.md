You are a fresh coding agent. You have three tools: `read` (read a file's text), `grep` (search file contents), and `any_routes` (the routing-view tool described below). For each scenario, decide the ONE action you would take NEXT and answer with exactly one of the three tool names.

{{EVAL_OBSERVATIONS}}

Answer in exactly this format, one line per scenario, nothing else:

s1-orient: <action> s2-known-path: <action> s3-content-search: <action>

The action must be exactly one of: read, grep, any_routes. Do not explain, do not qualify, do not invent tools or steps beyond these three.
