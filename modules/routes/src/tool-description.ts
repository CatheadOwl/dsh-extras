/**
 * Model-facing wording for the `any_nav` tool. Lives in its own module so
 * the row's loader entry stays on the loader contract while the
 * description-contract test keeps importing the single SSOT.
 */
export const ANY_ROUTES_DESCRIPTION =
  'Build a routing view from Markdown descriptions under a directory: folders are represented by their README.md (its description, when present). Use before exploring an unfamiliar Markdown knowledge base to pick a folder route. Route lines are always workspace-root-relative full paths (e.g. `guides/containment/containment.md`), never relative to the selected route root, and are sorted case-insensitively by route path. Every line is either a Markdown file route (with ` | description` when the file has one) or a depth-truncated folder rendered as `[truncated: N] folder-path` (with ` | description` when the folder\u2019s README has one), where N is the folder\u2019s recursive .md count (the total that would expand on descent); a folder with no Markdown content under it is omitted entirely, so `[truncated: 0]` never appears. In a `path | description` line, everything after the FIRST ` | ` is the description (it may itself contain ` | `); route paths never contain the separator. `routeCount` counts route entries (files plus truncated folders), not raw .md files or structural tree nodes. In the response, `root` is the absolute workspace root and `anchor` the absolute route root of this view; `depth` is measured from `anchor`, and the requested `routePath` is echoed when one was passed. A description comes from the document itself: frontmatter `description:`, else a `description:` line in its head, else its first substantive prose line. Traversal filters (excluded directory/file names, dot entries, gitignored paths) apply without an echo \u2014 a path absent from the view was excluded by policy or holds no Markdown; the only mid-scan cutoff, the maxFiles budget, always reports a warning in `diagnostics`. Returns route paths and descriptions, never file content.'
